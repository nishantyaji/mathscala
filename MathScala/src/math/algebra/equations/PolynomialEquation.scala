package math.algebra.equations

class PolynomialEquation(d: Int) {
  val degree = d
  // 0private var coefficients: Array[Double] = new Array[Double]degree]
  // def this(input: Array[Double]) = { coefficients = input } 
}